# Test-only dependencies. Install alongside requirements.txt for running
# the test suite (backend/tests/):
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# Not included in the Docker image's production install — see Dockerfile.
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
